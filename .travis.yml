language: node_js

node_js:
  - '12'

cache:
  directories:
    - ".eslintcache"
    - "node_modules"

script:
  - npm run test
  - npm run test:coverage
  - npm run build

after_success:
  - bash <(curl -s https://codecov.io/bash) -f coverage/coverage-final.json